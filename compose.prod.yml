services:
  discord-bot:
    container_name: discord-bot
    image: discord-bot
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - cachenet
      - services-proxy

networks:
  cachenet:
    external: true
  services-proxy:
    external: true
